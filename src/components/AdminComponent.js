import React from "react";
import { Button } from "reactstrap";

export default function AdminComponent() {
  return (
    <div>
      <Button>Add member</Button>
      <Button>Change member office</Button>
      <Button>Add Event</Button>
    </div>
  );
}
